# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment files may contain secrets (e.g. generated SASL credentials) —
# only the committed .env.*.example templates are safe to track.
.env
.env.*
!.env.*.example
